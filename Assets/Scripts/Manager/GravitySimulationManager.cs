using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GravitySimulationManager: MonoBehaviour
{
    //[SerializeField] public Entity entity;

    private void FixedUpdate()
    {
        //if (entity == null || entity.rbInitialPosition==null || entity.transform.position.z==entity.rbInitialPosition.z)
        //    return;
        //if (entity.zVelocity<0)
        //{

        //}
    }
}
